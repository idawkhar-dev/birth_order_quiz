cat > .replit << 'EOF'
[deployment]
run = ["bash", "-c", "pip install fastapi uvicorn pandas scikit-learn joblib && uvicorn main:app --host 0.0.0.0 --port 8080"]
deploymentTarget = "cloudrun"

[[ports]]
localPort = 8080
externalPort = 80
EOF